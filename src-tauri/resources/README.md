# Recursos opcionais do Bloquin

Coloque o binário do `arduino-cli` nesta pasta quando quiser empacotá-lo junto
com o aplicativo. O desenvolvimento também funciona com `arduino-cli` no
`PATH`, pela variável `ARDUINO_CLI_PATH` ou pelo download automático do app.
